---
filebeat_version: "7.14.0"
filebeat_install_type: remote
elastic_instance: "{{ hostvars['el-instance']['ansible_facts']['default_ipv4']['address'] | default('0.0.0.0') }}"
kibana_instance: "{{ hostvars['k-instance']['ansible_facts']['default_ipv4']['address'] | default('0.0.0.0') }}"